export interface AuthFormValues {
  fullName?: string;
  email: string;
  password: string;
  cPassword?: string;
}
export interface ContributeFormValues {
  contributeTo: string;
  amount: string;
  coin: string;
}
